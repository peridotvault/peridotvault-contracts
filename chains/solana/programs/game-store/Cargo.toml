[package]
name = "game-store"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "game_store"

[features]
alloc = ["quasar-lang/alloc"]
debug = ["quasar-lang/debug"]
client = []
default = []

[dependencies]
quasar-lang = "0.0.0"
quasar-spl = "0.0.0"
